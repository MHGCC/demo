spring.application.name=demo
# 开启 devtools 热部署（默认已开启，以下为常用可选配置）
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
spring.devtools.restart.exclude=static/**,public/**,resources/**,META-INF/**
